'use strict';

angular.module('campaignTrackerApp')
  .config(function ($stateProvider) {
    /*
     * disable admin route for now. may add into dashboard later
    $stateProvider
      .state('admin', {
        url: '/admin',
        templateUrl: 'app/admin/admin.html',
        controller: 'AdminCtrl'
      });
    */
  });
